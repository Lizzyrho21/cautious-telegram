import React from 'react'

import { useEffect } from 'react';

const Playlist = ({tracks}) => {

    
        // const fetchPlaylist =  () => {
            
        //     try {
        //         tracks.map((track) => { 
        //       const { data } = playlistGenerator(track); //destructuring to access the data property of the axios response.
        //         data.tracks.items.map((track) => {
                    
        //             console.log(track.artists.map((el) => el.name)  )
        //             console.log(track.name)
        //             console.log(track.uri);})

        //       }) // setting the state of profile data
                
        //         } catch (e) {
        //         console.error(e);
        //         }
        //     }
        //     fetchPlaylist();
            

            
        
            
      

  return (
    <div>Playlist</div>
  )
}

export default Playlist